<nav class="navbar navbar-inverse no-margin-bottom">
  <div class="container-fluid">
    <div class="navbar-header col-sm-3 navbar-left">
      <button class="navbar-toggle" type="button" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="https://trungquandev.com/" target="__blank">
        <img src="images/logo_favicon/logo-trungquandev.png" alt="logo-trungquandev" />
      </a>
    </div>
    <div class="col-sm-3 no-padding">
      <ul class="nav navbar-nav navbar-left no-padding-start">
        <li><input class="searchBox" type="text" name="search" placeholder="Tìm kiếm tên cuộc trò chuyện" />
          <div id="search-results">
            <h3>Kết quả</h3>
            <div class="search_content">
              <ul></ul>
            </div>
          </div>
        </li>
      </ul>
    </div>

    <div class="collapse navbar-collapse col-sm-6 navbar-right" id="myNavbar">
      <ul class="nav navbar-nav navbar-right">
        <li><a href="/" title="Trang chủ"><i class="fa fa-home"></i></a></li>
        <li>
          <a id="show-modal-contacts" href="#contactsModal" data-toggle="modal" title="Quản lý liên lạc">
            <i class="fa fa-user-plus"></i>
            <div class="noti_contact_counter">
              <% if(countAllContactsSent + countAllContactsReceived>0 ){ %>
                <%= countAllContactsSent + countAllContactsReceived %>
              <% } %>
            </div>
          </a>
        </li>

        <li>
          <a href="javascript:void(0)" id="noti_Button" href="#" data-toggle="modal" title="Thông báo"><i
              class="fa fa-globe"></i>

            <!-- render number notif -->
            <% if(countNotifUnread> 0) { %>
              <div class="noti_counter">
                <%= countNotifUnread %>
              </div>
            <% } else { %>
              <div class="d-none noti_counter">
              </div>
            <% } %>
              <div id="noti_Container">
                <div id="notifications">
                  <a href="javascript:void(0)" id="popup-mark-notif-as-read">Đánh dấu tất cả đã đọc</a>
                  <div class="noti_content">
                    <!-- render notifications from server -->
                    <% notifications.forEach(function(notif){ %>
                      <%- notif %>
                    <% }); %>
                  </div>
                  <div class="seeAll"><a href="#notificationsModal" data-toggle="modal">Xem tất cả</a></div>
                </div>
              </div>
          </a>
        </li>

        <li id="dropdown-navbar-user" class="dropdown" data-uid='<%= user._id %>'>
          <a href="#" class="dropdown-toggle" data-toggle='dropdown'>
            <img class="avatar-small" id="navbar-avatar" src="images/users/<%= user.avatar %>" alt="" />
            <span id="navbar-username"><%= user.username %></span>
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
            <li>
              <a href="#user-profile-modal" data-toggle="modal">
                <i class="fa fa-cog"></i>
                &nbsp;Cài đặt tài khoản
              </a>
            </li>
            <li><a href="/auth/logout"><i class="fa fa-sign-out"></i> &nbsp;Đăng xuất</a></li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>